# Thesis Web

靜態網站版本，適合直接放到 GitHub Pages。

## 結構

- `index.html`：首頁
- `styles.css`：樣式
- `app.js`：前端互動
- `data/theses.json`：論文資料

## 本機預覽

可在此資料夾啟動簡單靜態伺服器，例如：

```bash
cd /Users/jack80057/Desktop/codex/thesis-web
python3 -m http.server 8000
```

然後打開 `http://localhost:8000`。
